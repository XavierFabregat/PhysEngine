/**
 * PhysEngine - Canvas debug renderer entry point.
 *
 * Browser-only: depends on DOM types, so it is kept out of the main entry.
 *
 * @example
 * import { CanvasRenderer } from '@xavifabregat/physengine/canvas';
 */

export { CanvasRenderer } from './debug/examples/CanvasRenderer.js';
